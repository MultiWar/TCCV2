import React, {useEffect} from 'react';

import { Container } from './styles';

const userHeaderInformation: React.FC = () => {

    useEffect(() => {
        
    }, [])

    return (
        <Container></Container>
    );
}

export default userHeaderInformation;